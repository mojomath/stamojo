repos:
  - repo: local
    hooks:
      - id: mojo-format
        name: mojo-format
        entry: pixi run mojo format
        language: system
        files: '\.(mojo|🔥|py)$'
        stages: [pre-commit]
